function mixer(){
    for (i=1; i < Array.length + 1; i++){
        let pronoun = ['the','our'];
        let adj = ['great', 'big' ];
        let noun = ['jogger','racoon'];

        console.log(sentence);
    }
};

var sentence = pronoun+adj+noun;

window.onload 